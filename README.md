Install (Установка)

git clone https://github.com/zdeadfoxx/laravel-save-text_and_uploud-files.git

composer install

yarn or npm install

php artisan migrate

php artisan key:generate

php artisan serve
